import { ProgramInspector } from "@/components/ProgramInspector";

export const metadata = { title: "Program Inspector — Solaris" };

export default function InspectPage() {
  return <ProgramInspector />;
}
